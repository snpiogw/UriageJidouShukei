# Codex instructions

## 目的

このファイルは、このリポジトリで作業するCodex向けの短い案内です。不要な全体再調査を避け、確定済みの設計と現在地点を再利用してください。

## 作業開始時

次の順で必要な情報だけを確認します。

1. この `AGENTS.md`
2. `docs/CURRENT_STATE.md`
3. 設計理由が必要な場合だけ `docs/DECISIONS.md`
4. 今回の変更に直接関係するコードとテスト
5. 必要な場合だけ周辺コード

ドキュメントと実装が矛盾する場合は、実装とテストを正としてドキュメントを更新します。

## プロジェクト方針

- Java 21 / Spring Boot / Maven / PostgreSQLの既存構成を尊重する。
- JPA、Spring Batch、Quartz、Flyway、Thymeleaf、Spring Securityの責務分離を維持する。
- 依頼と関係のない置換や大規模リファクタリングを同時に行わない。
- 秘密情報、`.env`、サービスアカウントJSONをGitへ追加しない。
- DBスキーマ変更は既存Flywayファイルを書き換えず、新しいマイグレーションで行う。
- 画面や外部出力へスタックトレース・内部例外・秘密情報を表示しない。

## 調査と実装

- まず `rg` で対象を絞り、必要な範囲だけ読む。
- 同じファイルや確定済み仕様を理由なく再調査しない。
- 不具合は、症状確認、原因特定、最小修正、関連テスト、回帰確認の順で扱う。
- 既存の命名、パッケージ、例外処理、バリデーション、テスト方式に合わせる。
- ユーザーの既存変更と秘密ファイルを保持する。

## テスト

- 変更箇所の関連テストから始め、最後に `./mvnw verify` を実行する。
- DB・Flyway・Quartz変更は、可能ならTestcontainersを含む統合テストで確認する。
- UI変更はMockMvcに加え、ローカルDocker環境で表示と主要導線を確認する。
- Google Sheets連携変更は、テスト用Sheetで入力、結果、エラーログを確認する。

## 引き継ぎ資料

- `docs/CURRENT_STATE.md` は作業ログではなく、現在確定している状態だけを記録する。
- 古い状態は追記せず置き換え、200行以内を目安に保つ。
- `docs/DECISIONS.md` は重要な設計判断と理由だけを記録し、細かな変更は残さない。
- 作業終了時に、完成したもの、未完了事項、既知の問題、次の作業を簡潔に更新する。
